export default function ExampleBody({ children }: { children: React.ReactNode }) {
    return (
        <div className= "bg-[#FFFFFF1A]  text-wrap rounded-md text-[0.98rem] leading-5 mb-4 mt-4 p-3" >
        { children }
        </div>
    )
}
