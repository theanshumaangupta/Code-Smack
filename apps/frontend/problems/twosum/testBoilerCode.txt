const array = #INPUT_1#
const target = #INPUT_2#
const output = #OUTPUT#
const result = twoSum(array, target)
if (JSON.stringify(result.sort()) !== JSON.stringify(output.sort())) {
	wrong(`Expected : ${output} \nYour output : ${result}`);
	return;
}
